class BrurteForce{
  private combinedGain;

  public String run(int[] data){
    int start = 0;
    int commonGain = 0;
    int[] gains = new int[];
    int[] gains = new int[];
    while(start < data.length - 1){
     for(int i = start; i < data.length(); ++i){
       
     }
      ++start;
    }
  }
}
